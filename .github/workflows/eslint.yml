name: Eslint
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v2
      with:
        node-version: '16.x'
    - name: setup bot
      run: |
        cd src/bot
        npm ci
    - name: run eslint
      run: |
        npm run lint